var expect = require('expect.js');

var Drag = require('../src/drag'),
    actions = require('../src/actions');

describe("highlighting rules", function () {
    describe("update in adjacent cell (horizontal)", function () {
    });

    describe("update in adjacent cell (vertical)", function () {
    });

    describe("update in adjacent cell (diagonal)", function () {
    });

    describe("update in non-adjacent cell", function () {
    });
});

describe("marking rules", function () {
    describe("start and stop within cell", function () {
    });

    describe("start and stop in different cells", function () {
    });
});
